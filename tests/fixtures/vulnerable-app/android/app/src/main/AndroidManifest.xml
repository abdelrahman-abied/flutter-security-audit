<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_SMS"/>

    <!--
      A commented-out block is not a declaration. Neither permission below may
      be reported, and the only way to know that is to track the comment block
      across lines: the inner lines carry no marker of their own.
      <uses-permission android:name="android.permission.READ_CALL_LOG"/>    EXPECT-MISS
      <uses-permission android:name="android.permission.RECORD_AUDIO"/>     EXPECT-MISS
    -->

    <application
        android:label="vulnerable_fixture"
        android:allowBackup="true"
        android:debuggable="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- <activity android:name=".Old" android:exported="true" />       EXPECT-MISS -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="fixtureapp"/>
            </intent-filter>
        </activity>
    </application>
</manifest>

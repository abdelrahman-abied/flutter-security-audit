import Flutter
import UIKit

@main
class AppDelegate: FlutterAppDelegate {
    // Readable while the device is locked, and restored to a new device.
    let accessibility = kSecAttrAccessibleAlways
}
